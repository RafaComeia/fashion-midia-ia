# Story 1.3: Autenticação e Cadastro de Usuário

## Status
InReview

## Executor Assignment
```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["jest", "supertest", "tsc"]
```

## Story
**As a** visitante da plataforma,
**I want** me cadastrar e fazer login com e-mail/senha ou conta Google,
**so that** possa acessar meu workspace com segurança e ter minha sessão persistida.

## Acceptance Criteria
1. Cadastro via e-mail/senha funcional com validação: e-mail válido, senha mínima 8 caracteres, campos obrigatórios
2. Login via e-mail/senha funcional retornando JWT válido e sessão Supabase
3. Login via OAuth Google funcional com redirect correto após autenticação
4. Sessão persistida com refresh token automático via Supabase Auth (sem logout ao fechar e reabrir o browser)
5. Tela de "Esqueci minha senha" com envio de e-mail de recuperação via Supabase Auth
6. Após primeiro login (novo usuário), redirecionar para `/onboarding`
7. Após login de usuário existente com workspace, redirecionar para `/dashboard`
8. Middleware Next.js protege todas as rotas do grupo `(dashboard)` — redireciona para `/login` se não autenticado
9. Endpoint `POST /auth/sync` no backend sincroniza sessão Supabase e retorna perfil do usuário

## 🤖 CodeRabbit Integration

### Story Type Analysis
**Primary Type**: Security / Authentication  
**Secondary Type(s)**: API, Frontend  
**Complexity**: Medium

### Specialized Agent Assignment
**Primary Agents**:
- @dev: Implementação frontend e backend de auth
- @architect: Revisão do fluxo de segurança

**Supporting Agents**:
- @qa: Testes de segurança (JWT validation, proteção de rotas)

### Quality Gate Tasks
- [ ] Pre-Commit (@dev): Testes unitários de auth passando (login, cadastro, middleware)
- [ ] Pre-Commit (@dev): Nenhum token ou secret exposto no código
- [ ] Pre-PR (@devops): Testar fluxo completo de login em staging

### Self-Healing Configuration
**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL + HIGH

**Predicted Behavior**:
- CRITICAL issues: auto_fix (ex: token exposto, validação ausente)
- HIGH issues: document_only

### CodeRabbit Focus Areas
**Primary Focus**:
- Nenhum token JWT ou API key hardcoded no código frontend
- Validação de input em todos os formulários (client + server side)

**Secondary Focus**:
- OWASP: proteção contra XSS em formulários
- Middleware de autenticação cobre todas as rotas protegidas

## Tasks / Subtasks

- [x] Configurar Supabase Auth no projeto (AC: 1, 2, 3)
  - [x] Instalar `@supabase/supabase-js` e `@supabase/ssr` em `apps/web`
  - [x] Criar `apps/web/lib/supabase/client.ts` (browser client)
  - [x] Criar `apps/web/lib/supabase/server.ts` (server client para RSC)
  - [x] Criar `apps/web/lib/supabase/middleware.ts` (helper para middleware)
  - [x] Configurar variáveis: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- [x] Criar telas de autenticação no frontend (AC: 1, 2, 3, 5)
  - [x] Criar `apps/web/app/(auth)/login/page.tsx` com form email/senha + botão Google
  - [x] Criar `apps/web/app/(auth)/cadastro/page.tsx` com form de cadastro
  - [x] Criar `apps/web/app/(auth)/recuperar-senha/page.tsx` com form de e-mail
  - [x] Criar `apps/web/app/(auth)/layout.tsx` (layout sem sidebar)
  - [x] Validação client-side com `react-hook-form` + `zod`
- [x] Implementar OAuth Google (AC: 3)
  - [x] Configurar provider Google no painel Supabase
  - [x] Criar `apps/web/app/(auth)/callback/route.ts` para tratar o redirect OAuth
  - [x] Botão "Entrar com Google" usando `supabase.auth.signInWithOAuth`
- [x] Implementar sessão persistida (AC: 4)
  - [x] Criar `apps/web/middleware.ts` usando `@supabase/ssr` para refresh automático
  - [x] Garantir que cookies de sessão são atualizados em cada request
- [x] Implementar redirecionamentos pós-login (AC: 6, 7)
  - [x] Verificar `user_metadata.workspace_id` após login
  - [x] Se sem workspace → redirect `/onboarding`
  - [x] Se com workspace → redirect `/dashboard`
- [x] Implementar middleware de proteção de rotas (AC: 8)
  - [x] `apps/web/middleware.ts` verifica sessão para rotas `/(dashboard)/*`
  - [x] Redireciona para `/login?redirect=<url>` se não autenticado
  - [x] Após login bem-sucedido, redireciona para a URL original
- [x] Implementar endpoint `POST /auth/sync` no backend (AC: 9)
  - [x] Criar `apps/api/src/modules/auth/auth.module.ts`
  - [x] Criar `apps/api/src/modules/auth/auth.controller.ts`
  - [x] Endpoint valida JWT Supabase via `SUPABASE_JWT_SECRET`
  - [x] Criar guard `JwtAuthGuard` para proteger endpoints da API
- [x] Testes (AC: todos)
  - [x] Teste unitário: `auth.controller.spec.ts` — sync endpoint
  - [x] Teste unitário: `jwt-auth.guard.spec.ts`

## Dev Notes

### Fluxo de autenticação
```
Usuário → /login → Supabase Auth
                        │
              ┌─────────┴──────────┐
              │ email/senha        │ Google OAuth
              ▼                    ▼
         signInWithPassword    signInWithOAuth
              │                    │
              └─────────┬──────────┘
                        │ JWT + refresh token (cookie)
                        ▼
                  middleware.ts (verifica sessão)
                        │
              ┌─────────┴──────────┐
              │ tem workspace_id?  │
              ▼                    ▼
          /dashboard          /onboarding
```

### Clientes Supabase (Next.js App Router)
```typescript
// lib/supabase/client.ts — uso em Client Components
import { createBrowserClient } from '@supabase/ssr'
export const createClient = () =>
  createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!)

// lib/supabase/server.ts — uso em Server Components e Route Handlers
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'
export const createClient = () => createServerClient(...)
```

### Validação do JWT no backend (NestJS)
```typescript
// O backend valida o JWT do Supabase usando o JWT_SECRET do projeto Supabase
// Variável necessária: SUPABASE_JWT_SECRET (disponível nas configurações do projeto Supabase)
```

### Variáveis de ambiente necessárias
```bash
# apps/web
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# apps/api
SUPABASE_JWT_SECRET=     # JWT secret do projeto Supabase (Settings > API)
SUPABASE_SERVICE_ROLE_KEY=
```

### Referências de arquitetura
- `docs/architecture/architecture.md` — Seção 3.3: Fluxo de Autenticação Frontend
- Supabase Auth com `@supabase/ssr` para Next.js App Router
- `apps/web/middleware.ts` protege grupo de rotas `(dashboard)`

### Testing
- Framework: Jest + Testing Library (frontend), Jest + Supertest (backend)
- Testes obrigatórios:
  - `auth.controller.spec.ts`: POST /auth/sync com JWT válido e inválido
  - `jwt-auth.guard.spec.ts`: guard rejeita requests sem token
- Localização: ao lado dos arquivos (`.spec.ts`)

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-02 | 1.0 | Story criada | River (@sm) |
| 2026-04-07 | 1.1 | Status: Draft → Ready (GO 10/10) | Pax (@po) |
| 2026-04-12 | 1.2 | Implementação completa — Status: Ready → InReview | Dex (@dev) |

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### File List
**Criados:**
- `apps/web/lib/supabase/client.ts`
- `apps/web/lib/supabase/server.ts`
- `apps/web/lib/supabase/middleware.ts`
- `apps/web/middleware.ts`
- `apps/web/app/(auth)/layout.tsx`
- `apps/web/app/(auth)/login/page.tsx`
- `apps/web/app/(auth)/cadastro/page.tsx`
- `apps/web/app/(auth)/recuperar-senha/page.tsx`
- `apps/web/app/(auth)/callback/route.ts`
- `apps/api/src/modules/auth/auth.module.ts`
- `apps/api/src/modules/auth/auth.controller.ts`
- `apps/api/src/modules/auth/guards/jwt-auth.guard.ts`
- `apps/api/src/modules/auth/auth.controller.spec.ts`
- `apps/api/src/modules/auth/guards/jwt-auth.guard.spec.ts`

**Modificados:**
- `apps/api/src/app.module.ts` (importa AuthModule)
- `apps/api/src/main.ts` (void bootstrap() para lint)

### Completion Notes
- shadcn/ui não instalado — formulários usam Tailwind puro (consistente com login page existente)
- Teste de integração e2e não implementado nesta story (dependência de ambiente Supabase em staging)
- `POST /auth/sync` retorna `{ synced, userId, email }` — expandível na Story 1.4 quando perfil de workspace for criado

## QA Results

**Revisor:** Quinn (@qa) | **Data:** 2026-04-13 | **Verdict:** PASS_WITH_CONCERNS

### Acceptance Criteria
- AC1–AC8: PASS completo
- AC9: PARCIAL — retorna `{ synced, userId, email }` em vez do perfil completo. Aceitável, expandir na Story 1.4.

### Testes
- 9/9 passando. Lint limpo. Cobertura adequada para guard e controller.

### Concerns (não bloqueantes)

| Severidade | Descrição | Ação |
|-----------|-----------|------|
| MEDIUM | `SyncUserDto` é interface sem class-validator — sem validação server-side | Converter para class na Story 1.4 |
| MEDIUM | `JwtModule.register` síncrono — secret pode ser `undefined` se env carregada assincronamente | Migrar para `JwtModule.registerAsync` |
| LOW | `signOptions.expiresIn` semanticamente incorreto (módulo só valida, não assina) | Remover signOptions |
| LOW | Sem teste e2e do fluxo completo | Implementar em Sprint 2 com staging |

### Decisão
**APROVADO para push.** Nenhum bloqueador. Concerns MEDIUM a endereçar na Story 1.4.
